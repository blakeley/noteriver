// Player starts before the actual MIDI content to provide a lead-in
export const PLAYER_INITIAL_TIME_OFFSET = -1;

// Thumbnail mode starts at a different position to show content immediately
export const THUMBNAIL_INITIAL_TIME = 2;
